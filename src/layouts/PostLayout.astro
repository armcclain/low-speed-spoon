---
const { frontmatter } = Astro.props;
---
<Layout title={`${frontmatter.title} — the low-speed spoon`} theme="theme-library">
  <article class="mx-auto max-w-3xl px-4 py-12 prose">
    <h1>{frontmatter.title}</h1>
    {frontmatter.date && <p class="text-sm text-gray-500">{new Date(frontmatter.date).toLocaleDateString()}</p>}
    <slot />
  </article>
</Layout>

---
import Layout from './BaseLayout.astro';
